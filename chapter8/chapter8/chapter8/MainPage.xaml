<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:chapter8"
             mc:Ignorable="d"
             x:Class="chapter8.MainPage"
             Padding="10"
             Title="Chapter8 Demos">

    <TableView Intent="Menu">
        <TableRoot>
            <TableSection>
                <TextCell Text="XAML Custom Button Renderer"
                          Detail="Implement a Custom Button Renderer"
                          Command="{Binding NavigateCommand}"
                          CommandParameter="{x:Type local:CustomButtonXaml}" />
                <TextCell Text="XAML Text Validator Effect"
                          Detail="Implement a Text Validator Effect"
                          Command="{Binding NavigateCommand}"
                          CommandParameter="{x:Type local:TextValidatorEffectXaml}" />
                <TextCell Text="XAML Native Views"
                          Detail="Implement a Native Views"
                          Command="{Binding NavigateCommand}"
                          CommandParameter="{x:Type local:NativeViewXaml}" />
                
            </TableSection>
        </TableRoot>
    </TableView>

</ContentPage>
